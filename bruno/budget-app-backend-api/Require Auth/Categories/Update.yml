info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/categories/1
  body:
    type: json
    data: |-
      {
        "name": "Test Update Category"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
