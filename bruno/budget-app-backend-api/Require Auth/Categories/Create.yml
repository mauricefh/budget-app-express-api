info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/categories
  body:
    type: json
    data: |-
      {
        "name": "Test Create Category"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
