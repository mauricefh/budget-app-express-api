info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/accounts
  body:
    type: json
    data: |-
      {
        "name": "TD",
        "type": "checking"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
