info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/accounts/5
  body:
    type: json
    data: |-
      {
        "name": "KOHO",
        "type": "credit"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
